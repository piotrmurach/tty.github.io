name: TTY
pygments: true
assets:
  baseurl: ../assets/
  compress:
    css: sass
    js: uglifier
  cachebust: hard
  gzip: [ text/css, application/javascript ]
exclude: [components, Rakefile, Gemfile, Gemfile.lock]
